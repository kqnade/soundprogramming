# Week 8: アナログフィルタ — 学習ノート

> 出典: `~/Downloads/Wk8-AnalogFilter.pdf` (SILAA, Victor Alex, 釧路工業高等専門学校 / 2026-06-15)
> 教科書 第6章が対応範囲。目的キーワード: 折返し「aliasing」, サンプリング定理, 伝達関数, DFTフィルタ, overlap-add。

## 1. アナログフィルタの定義

アナログフィルタとは、入力信号の周波数成分のうち「特定の周波数帯域」を通過させ、
それ以外を減衰させる**アナログ（連続時間）回路**。

- 入出力はどちらも連続時間信号 `x(t), y(t)`
- 抵抗 R・コイル L・コンデンサ C などの受動素子（またはオペアンプ等の能動素子）で構成
- 周波数に依存した振幅・位相特性を持つのが本質
- 同義的に「連続時間フィルタ」とも呼ぶ

---

## 2. LPF / HPF / BPF / BEF・BRF の違い

| 略語 | 名称 | 通す帯域 | 特徴 |
|------|------|----------|------|
| LPF | Low-Pass Filter（低域通過） | `0 ≤ |ω| ≤ ωc` | 高周波を遮断。一番基本。 |
| HPF | High-Pass Filter（高域通過） | `|ω| ≥ ωc` | 低周波・DCを遮断。 |
| BPF | Band-Pass Filter（帯域通過） | `ω1 ≤ |ω| ≤ ω2` | 中央の帯域だけ通す。 |
| BEF / BRF | Band-Elimination / Reject Filter（帯域除去） | `ω1 ~ ω2` を除去 | 特定帯域だけを切りたい場合（例: ノッチフィルタ）。 |

要点:
- どれを通し、どれを遮断するかの「位置」が違うだけ
- LPF と HPF を直列に接続すれば BPF になる
- LPF と HPF を並列に接続し出力を足せば BEF になる

---

## 3. 基本用語

- **pass（通過）**: 入力の強度を保持したまま出力
- **rejection / elimination（阻止・除去）**: 入力の強度を減衰させて出力
- **cut-off frequency `ωc`（遮断周波数）**: 通過／阻止の境界の周波数
  - 電力的に「伝達関数の最大値 1 の半分 1/2」を境界と考える（= 電圧の `1/√2`, -3dB 点）
- **frequency band / range（周波数帯域）**: 特定の周波数の範囲
  - 使い分け: 数値で指定（例: 20Hz–20kHz）→ **range**, 名称で指定（例: 5G, TV）→ **band**
- **lower / higher frequency range**: 特定の周波数以下／以上の帯域

---

## 4. なぜフィルタでノイズが減るのか

- ノイズは多くの場合「広い周波数帯にまたがる」あるいは「目的帯域外に主成分がある」
- 目的信号の帯域だけを残す LPF/BPF を入れれば、帯域外にあるノイズ成分のパワーを削れる
- 例:
  - 人の声はだいたい 300Hz ~ 3.4kHz。これを LPF にかけると、高域の雑音（ヒスなど）が除去される
  - 50/60Hz のハムは HPF で十分低くすれば除去可能
- フィルタの設計 = 信号とノイズの周波数がどれだけ分離できるかを活用すること

---

## 5. A/D 変換の前にアナログ LPF が必要な理由

A/D 変換ではサンプリング周波数 `fs` で標本化する。
サンプリング定理により、`fs/2` より高い成分を持つ信号はうまく復元できない。

- 信号に `fs/2` を超える高周波成分があると、折り返し（エイリアシング）が起きて
  低周波成分に混入してしまい、復号できなくなる
- だから **A/D 変換器の前にアナログ LPF（アンチエイリアシングフィルタ）** を置いて
  `f > fs/2` の成分を十分減衰させてからサンプリングする
- デジタルフィルタでは折返し後に起きたエイリアスを取り除けないので、アナログ段階が必須

---

## 6. 折返し（folding）とエイリアシング（alias）

サンプリングによって、内容が `fs` の周期で周波数軸上に複製される。
元の周波数 `f` が `fs/2` を超えると、その複製（鏡像）が `0 ~ fs/2` の領域に折り返し,
異なる周波数のように現れる。これが **alias（偽の周波数）**。

- 折返し先の周波数: `f_alias = |f - k·fs|`（`k` は `f_alias` が `0 ~ fs/2` に収まる整数）
- 例: `fs = 8kHz`, 入力 `f = 5kHz` → `f_alias = |5 - 8| = 3kHz`。3kHzかのように誤認
- 一度起きると後段の処理で取り除けない = A/D の前のアナログ LPF が重要

---

## 7. 標本化定理（サンプリング定理）

- 信号の最高周波数 `fmax` に対して、サンプリング周波数 `fs > 2·fmax`（かつ > を厳密に満たす）を満たせば、元信号はサンプル列から完全に復元できる
- `fs / 2` を **ナイキスト周波数** と呼ぶ
- 実用上は「ナイキスト周波数以上の成分をアンチエイリアスフィルタで落としてからサンプリング」がルール

例: 可聴域 ~20kHz を扱うなら `fs = 44.1kHz`。ナイキスト 22.05kHz が上限。

---

## 8. 伝達関数 `H(ω) = Vout(ω) / Vin(ω)`

- アナログフィルタは要するに交流電気回路。入出力特性は伝達関数で定義
- 伝達関数 = 周波数領域での入出力の比率
- `V(ω)` は交流回路の「電圧フェーザ」であり、信号処理のスペクトル成分 `X(ω)` や `X_k` と同義
- 角周波数 `ω` は、周波数 `f` に読み替えても OK
- `|H(ω)|`: 振幅特性（ゲイン）
- `∠H(ω)`: 位相特性
- LPF なら `ω → ∞` で `|H| → 0`、`ω = 0` で最大。HPF はその逆、となるように設計
- 伝達関数は「ディジタルフィルタ」でも共通概念として使われる

---

## 9. RL 直列回路で LPF / HPF ができる理由

RL 直列回路（R と L を直列、Vin を両端に加える）を考える。
インダクタのインピーダンス: `ZL = jωL`、抵抗: `R`。

・**R の電圧を出力** → LPF
- `H(ω) = Vr / Vin = R / (R + jωL)`
- ω → 0 で `ZL → 0` なので R だけで分圧され `|H| → 1`
- ω → ∞ で `ZL → ∞` なので R に電圧が回らなくなり `|H| → 0`
- 低域通過

・**L の電圧を出力** → HPF
- `H(ω) = VL / Vin = jωL / (R + jωL)`
- ω → 0 で `VL → 0`。ω → ∞ で `VL → Vin`。
- 高域通過

見方: 「周波数が高くなると L が電圧を独占する」結果、L 側は高域を取り、R 側は低域を取る。

---

## 10. RL 回路のカットオフ周波数 `ω0 = R / L` の導出

`H(ω) = R / (R + jωL)`（R出力・LPFの例）のゲイン:

```
|H(jω)| = R / √(R² + (ωL)²)
        = 1 / √(1 + (ωL/R)²)
```

カットオフは `|H| = 1/√2` となる点:

```
1 / √(1 + (ω0 L/R)²) = 1/√2
1 + (ω0 L / R)²     = 2
(ω0 L / R)²         = 1
ω0 L / R            = 1
ω0 = R / L
```

同様に HPF 側 `H(ω) = jωL / (R + jωL)` の `|H|` も同じ `ω0 = R/L` で `1/√2` になる。

---

## 11. DFT フィルタの手順

離散フーリエ変換を利用した周波数領域フィルタ（FIR などでなく FFT を活用する方法）:

1. 信号 `x[n]`（長さ N）を FFT → `X[k]`
2. 所望の周波数応答 `H(ω)` を N 点で標本化 → `H[k]`
3. スペクトル積: `Y[k] = X[k] · H[k]`
4. `Y[k]` を IFFT → 出力 `y[n]`

これは循環畳み込み `y = x ⊛ h` に相当する。`h[n] = IFFT{H[k]}` がインパルス応答。

---

## 12. DFT フィルタの問題点

- **区間境界の不連続（周期化的な跳び）** :
  - DFT は信号を N 周期とみなすので、信号の両端がつながらないと周波数軸にリーケージが起きる
- **プツプツ音** :
  - フレームごとに別の波形を合成する結果、ブロック境界で波形が途切れてクリックノイズが出る
- **レイテンシ** :
  - まとまった N サンプルを溜めないと計算できないため、入力から出力まで N サンプル分の遅延が発生
  - リアルタイム用途（ライブ音響など）には致命的

---

## 13. zero-padding と overlap-add（対策）

**zero-padding（ゼロ詰め）**:
- 信号 `x[n]`（長さ L）とインパルス応答 `h[n]`（長さ M）の線形畳み込みは長さ `L+M-1` になる
- DFT の循環畳み込みは周期 N なので、`N ≥ L+M-1` までゼロ詰めすれば循環畳み込みと線形畳み込みが一致する
- これで両端の「折返し成分」による歪みを防げる

**overlap-add（オーバーラップ加算法）**:
- 入力をブロック分解し、各ブロックを個別に zero-pad → FFT → スペクトル積 → IFFT
- ブロックを隣接させるだけでなく、M-1 サンプルだけ**オーバーラップして足し合わせる**
- オーバーラップ部分は破棄せず、そこに次ブロックの先頭成分が重なるので足し合わせて復元
- 連続的な出力が得られ、ブロック境界のプツプツも消える
- もう一つの代表手法に **overlap-save** がある

---

## 14. リアルタイム処理では時間領域フィルタが重要

- リアルタイムでは「入ってくるサンプルを即座に処理する」必要がある
- 周波数領域（DFT）フィルタは N サンプル蓄えないと計算できない → 大きなレイテンシ
- 時間領域の **FIR / IIR** なら、1 サンプルごとに演算でき、遅延は最小（FIRなら次数サンプル分）
- したがってライブ用のイコライザやエフェクトはほぼ時間領域フィルタ
- 周波数領域フィルタは、オフライン処理や巨大なフィルタ長（畳み込みリバブなど）で bucktock 必要な場合に有用

---

## 15.（残項目）教科書 Chapter 6 を読む

教科書の該当テーマ（アナログフィルタ + 離散フーリエ変換の基礎）を該当章で確認すること。
特に以下を重点的に:
- 折返し周波数とアンチエイリアシング LPF の仕様
- RL/RC 回路の伝達関数とボード線図
- DFT によるフィルタ実装と overlap-add のブロック図