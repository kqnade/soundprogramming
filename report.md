# libpcm.so 解析レポート

作成日：2026-04-20
更新日：2026-04-20

---

## 1. 概要

`libpcm.so` は，非圧縮リニアPCM形式のWAVファイルを扱うためのCライブラリである．
WAVファイルのバイナリ入出力と，プログラムから扱いやすい実数値（-1.0〜+1.0）への相互変換を提供する．

- **対象フォーマット**：非圧縮リニアPCM，8/16/24/32 bit，1 or 2チャネル
- **WAVファイル構成**：RIFFヘッダ → fmtチャンク → dataチャンク
- **バイトオーダ**：リトルエンディアン（Intel CPU向け）
- **言語標準**：C99
- **戻り値の流儀**：0 = NULL = エラー

---

## 2. データ構造

### 2.1 Fmt 構造体（16バイト）

WAVファイルのfmtチャンクと同一レイアウト．

```c
typedef struct {
    unsigned short  type;   // 音声形式（1 = 非圧縮リニアPCM）
    unsigned short  ch;     // チャネル数（1 or 2）
    unsigned int    fs;     // 標本化周波数 [Hz]（例：44100, 48000）
    unsigned int    dr;     // データレート [B/s]（= fs × bs）
    unsigned short  bs;     // ブロックサイズ [B/標本]（= (bit/8) × ch）
    unsigned short  bit;    // 量子化深度 [bit/ch]（8, 16, 24, 32）
} Fmt;
```

### 2.2 Wav 構造体

PCMデータ全体を保持するコンテナ．fmtチャンク部分はWAVファイルと同形式．
独自拡張部分（`len`, `time`, `byte`, `val`）はファイルには記録されない．

```c
typedef struct {
    // fmtチャンク部分（WAVファイルと同形式）
    unsigned int    fmtSize;    // fmtデータサイズ（常に 16）
    Fmt             fmt;        // データ形式

    // dataチャンク部分（WAVファイルと同形式）
    unsigned int    size;       // dataバイト数
    unsigned char   *data;      // バイナリ標本列へのポインタ

    // 独自拡張部分
    unsigned int    len;        // 標本数
    double          time;       // 収録時間 [s]
    unsigned int    byte;       // バイト数／標本／チャネル（= bit/8）
    double          *val[2];    // 実数値標本配列（-1.0〜+1.0）
} Wav;
```

**バイナリ（data）と実数値（val）の二重保持：**

```
WAVファイル上          メモリ上
┌──────────┐   Read    ┌──────────────────────────────────────┐
│ バイナリ  │ ─────── → │ data[]（バイナリ）＋ val[]（実数値） │
│  data[]  │           │  dat2val() で変換済み                 │
└──────────┘   Write   └──────────────────────────────────────┘
                ← ──── val2dat() で実数値→バイナリ変換して出力
```

---

## 3. 関数解説

### 3.1 `pcmInit()` — Wav構造体の新規生成

```c
Wav *pcmInit(unsigned int bit, unsigned int ch, unsigned int fs, unsigned int len);
```

新規に音声データ用の領域を確保し，ゼロ初期化したWav構造体を返す．

**処理フロー：**

```
1. パラメータ検証
   - bit が 8 の倍数か
   - ch が 1 or 2 か
   - fs, len が 0 でないか

2. Fmt 構造体の構築
   bs = (bit/8) * ch
   fmt = {1, ch, fs, fs*bs, bs, bit}

3. Wav構造体のメモリ確保（malloc）

4. dataバッファの確保
   size = bs * len  →  data = calloc(bs, len)  ← ゼロ初期化

5. 独自拡張メンバの計算・確保
   len  = len
   time = len / fs
   byte = bs / ch
   val[c] = calloc(sizeof(double), len)  ← チャネル分，ゼロ初期化
```

**使用例（sin.c より）：**

```c
Wav *p = pcmInit(16, 1, 48000, 48000);   // 16bit，モノラル，48kHz，1秒分
for (int i = 0; i < len; i++) {
    double t = (double)i / fs;
    p->val[0][i] = sin(2.0 * M_PI * f * t);   // val[] に実数値を直接書込む
}
pcmWrite(stdout, p);   // 出力時に自動的にバイナリ化される
```

---

### 3.2 `pcmRead()` / `pcmLoad()` — WAVファイルの読込

```c
Wav *pcmRead(FILE *fp);       // FILE*から読込
Wav *pcmLoad(const char *fn); // ファイル名から読込（"-"で標準入力）
```

**WAVファイルのパース処理：**

```
WAVファイル（バイナリ）
│
├─[1] RIFFヘッダ確認
│      "RIFF"（4B）＋ サイズ（4B，スキップ）＋ "WAVE"（4B）
│
├─[2] fmtチャンク検索・読込
│      seekChunk("fmt ")  ← "fmt "が来るまで他チャンクをスキップ
│      fmtSize（4B）== 16 であることを確認（非圧縮PCMの条件）
│      Fmt 構造体を16B読込
│      type == 1（非圧縮），bit が 8 の倍数であることを確認
│
├─[3] dataチャンク読込
│      isChunk("data")
│      size（4B）読込
│      data = malloc(size)  →  fread(data, size)
│
└─[4] バイナリ → 実数値変換（dat2val）
       for i in 0..len:
         for c in 0..ch:
           val[c][i] = dat2val(data_ptr, byte)
           data_ptr += byte
```

**`dat2val()` の変換ロジック：**

| ビット深度 | バイナリ範囲 | 変換式 |
|-----------|------------|--------|
| 8bit | 0x00〜0xFF | `val = bin/128.0 - 1.0`（符号なし→オフセット補正） |
| 16bit | 0x8000〜0x7FFF | `val = bin/32768.0`（2の補数→負値補正） |
| 24/32bit | 同様 | 2の補数表現で負値処理 |

---

### 3.3 `pcmWrite()` / `pcmSave()` — WAVファイルの書出

```c
int pcmWrite(FILE *fp, const Wav *p);       // FILE*へ書出
int pcmSave(const char *fn, const Wav *p);  // ファイル名へ書出（"-"で標準出力）
```

**処理フロー：**

```
[1] 実数値 → バイナリ変換（val2dat）
     for i in 0..len:
       for c in 0..ch:
         val2dat(d, val[c][i], byte)
         d += byte

[2] WAVファイル書出
     "RIFF" (4B)
     totalSize = size + 36 (4B)   ← 36 = "WAVE"4 + fmtチャンク24 + "data"＋len 8
     "WAVE" (4B)
     "fmt " (4B) + fmtSize(4B) + fmt(16B)
     "data" (4B) + size(4B) + data(size B)
```

**`val2dat()` の変換ロジック（リミッタ付き）：**

| 条件 | 処理 |
|------|------|
| val <= -1.0 | DatMin（最小値）にクランプ |
| val >= +1.0 | DatMax（最大値）にクランプ |
| 8bit，通常値 | `val + 1.0` でオフセット後，バイナリ化 |
| 16/24/32bit 負値 | `val + 2.0` で補数表現化 |

> val[] の値域は **-1.0 <= x < +1.0** が適正範囲．超えると波形がクリップされる．

---

### 3.4 `pcmFin()` — メモリ解放

```c
void pcmFin(Wav *p);
```

`data`，`val[0]`，`val[1]`，`Wav`本体の順にfreeする．

---

### 3.5 `pcmInfo()` — 属性表示

```c
int pcmInfo(FILE *fp, const Wav *p);
```

チャネル数，標本化周波数，量子化深度，データサイズ，標本数，収録時間を表示する．デバッグ用途で `stderr` に渡すことが多い．

---

### 3.6 `pcmMusicFreq()` — 音名→周波数変換

```c
double pcmMusicFreq(const char *s);
```

| 入力例 | 出力 [Hz] |
|--------|----------|
| `"440"` | 440.0（数字列はそのままatof） |
| `"A4"` | 440.0（基準音） |
| `"A5"` | 880.0（1オクターブ上） |
| `"C4"` | 261.63（中央ド） |
| `"E-4"` | 311.13（ミ♭） |
| `"F+4"` | 369.99（ファ♯） |

計算式：`F_A4 * 2^(k/12 + (oct - 4))`（平均律）

---

## 4. サンプルアプリの解析

### 4.1 sin.c — 正弦波WAV生成

```
pcmInit(bit, ch, fs, len)
        ↓
val[0][i] = sin(ω * t)    # ω = 2π * f
        ↓
pcmWrite(stdout, p)
        ↓
pcmFin(p)
```

**コンパイル・実行：**

```bash
export LD_LIBRARY_PATH=./pcm/
cc sin.c -std=c99 -lpcm -lm -o sin
./sin | paplay            # 440 Hz（A4）
./sin 880 | paplay        # 880 Hz（A5）
./sin A3 | paplay         # 220 Hz（A3）
./sin C4 16 | paplay      # 中央ド，16bit
```

---

### 4.2 thru.c — 素通しフィルタ

```
pcmRead(stdin)
      ↓
（処理なし）
      ↓
pcmWrite(stdout, p)
      ↓
pcmFin(p)
```

ライブラリの基本的なI/Oパターンを確認するためのサンプル．入出力が一致すれば正常動作．

**コンパイル・実行：**

```bash
cc thru.c -std=c99 -lpcm -o thru
./thru < audio.wav | paplay
```

---

### 4.3 rate.c — 再生速度変更

```
pcmRead(stdin)
      ↓
p->fmt.fs *= rate    ← ここだけ！
      ↓
pcmWrite(stdout, p)
      ↓
pcmFin(p)
```

**コンパイル・実行：**

```bash
cc rate.c -std=c99 -lpcm -o rate
./rate 2.0 < audio.wav | paplay    # 2倍速
./rate 0.5 < audio.wav | paplay    # 0.5倍速
```

---

## 5. 再生速度変更の原理

### 5.1 仕組み

WAVファイルの `fs`（標本化周波数）は，「1秒間に何個の標本を再生するか」をプレイヤーに伝えるメタデータである．
**標本データは一切変更せず，ヘッダの `fs` だけを書き換える**ことで，再生速度を変えることができる．

```
元のWAV（fs = 48000 Hz）         変更後（fs = 96000 Hz）
標本数 = 48000（1秒分のデータ）   標本数 = 48000（変わらない）
                                  
プレイヤーの解釈：
「1秒間に48000標本を再生」 → 1秒で完了
「1秒間に96000標本を再生」 → 0.5秒で完了 = 2倍速！
```

### 5.2 速度と効果の一覧

| rate値 | 再生速度 | ピッチ変化 | 収録時間の変化 |
|--------|---------|-----------|-------------|
| 0.5 | 0.5倍速（遅く） | 1オクターブ下降 | 2倍に伸びる |
| 1.0 | 等速（変化なし） | 変化なし | 変化なし |
| 2.0 | 2倍速（速く） | 1オクターブ上昇 | 半分に縮む |
| 1.5 | 1.5倍速 | 短7度上昇 | 2/3に縮む |

### 5.3 注意事項

- この方法では**ピッチと速度が連動して変化**する（テープの早送りと同じ原理）
- ピッチを変えずに速度だけ変えたい場合（タイムストレッチ）や，速度を変えずにピッチだけ変えたい場合は，標本の補間・間引き処理が別途必要

### 5.4 実装の核心部分（rate.c:25）

```c
p->fmt.fs *= rate;   // 標本化周波数をスカラー倍するだけ
```

`pcmWrite()` が `p->fmt.fs` をそのままWAVヘッダに書き出すため，この1行だけで効果が得られる．
`p->fmt.dr`（データレート = fs × bs）は更新していないが，多くのプレイヤーは `fs` を優先するため実用上問題ない．

---

## 6. ライブラリのコンパイルと使用方法

```bash
# ライブラリのコンパイル
cc -c pcm.c -std=c99 -fpic -shared -o libpcm.so

# アプリのコンパイル（-lm は sin.c のみ必要）
cc sin.c  -std=c99 -lpcm -lm -o sin
cc thru.c -std=c99 -lpcm     -o thru
cc rate.c -std=c99 -lpcm     -o rate

# 実行時のライブラリパス設定
export LD_LIBRARY_PATH=./pcm/

# または Makefile を使う
make -C pcm
```

---

## 7. まとめ

| 関数 | 役割 |
|------|------|
| `pcmInit()` | 新規Wav構造体を確保・ゼロ初期化する |
| `pcmRead()` / `pcmLoad()` | WAVファイルを読み込み，バイナリを実数値に変換する |
| `pcmWrite()` / `pcmSave()` | 実数値をバイナリに変換してWAVファイルを書き出す |
| `pcmFin()` | Wav構造体のメモリを解放する |
| `pcmInfo()` | PCM属性をファイルに表示する |
| `pcmMusicFreq()` | 音名または数字列から周波数を返す |

このライブラリの設計上の特徴は，`val[]`（実数値）という独自拡張領域を設けることで，**ユーザはバイナリ形式を意識せずに -1.0〜+1.0 の実数で音声処理を記述できる**点にある．
バイナリとの相互変換は `pcmRead()`/`pcmWrite()` が自動的に行う．

---

## 8. PCM属性の違いによるWAVヘッダのバイナリ比較

### 8.1 sin2.c — 拡張サンプルプログラム

`sin.c` を改造し，量子化深度・チャネル数・標本化周波数をコマンドライン引数で指定できるようにした `sin2.c` を作成した．

```c
// 使い方：$ ./sin2 [周波数 [量子化深度 [チャネル数 [標本化周波数]]]]
// 例：
//   $ ./sin2 440  8 1 48000 > out_08bit_mono_48k.wav
//   $ ./sin2 440 16 2 48000 > out_16bit_stereo_48k.wav
//   $ ./sin2 440 16 1  8000 > out_16bit_mono_08k.wav
```

sin.c との差分は，`bit`・`ch`・`fs` をハードコードする代わりに `argv` から受け取る点のみ．

**コンパイル：**

```bash
export LD_LIBRARY_PATH=./pcm/
cc sin2.c -std=c99 -L./pcm -lpcm -lm -o sin2
```

### 8.2 生成したWAVファイル一覧

以下の7パターンのWAVファイル（440 Hz 正弦波，1秒）を生成した．

| ファイル名 | 量子化深度 | チャネル | fs [Hz] | ファイルサイズ |
|-----------|----------|---------|---------|-------------|
| out_08bit_mono_48k.wav |  8 bit | mono   | 48000 |  48 KB |
| out_16bit_mono_48k.wav | 16 bit | mono   | 48000 |  96 KB |
| out_24bit_mono_48k.wav | 24 bit | mono   | 48000 | 144 KB |
| out_32bit_mono_48k.wav | 32 bit | mono   | 48000 | 192 KB |
| out_16bit_stereo_48k.wav | 16 bit | stereo | 48000 | 192 KB |
| out_16bit_mono_08k.wav | 16 bit | mono   |  8000 |  16 KB |
| out_16bit_mono_44k.wav | 16 bit | mono   | 44100 |  88 KB |

### 8.3 WAVヘッダのバイト構造

WAVファイルの先頭44バイトが「RIFFヘッダ + fmtチャンク + dataチャンク先頭」に対応する．

```
オフセット  サイズ  内容
 0 –  3     4B    チャンクID  "RIFF"
 4 –  7     4B    RIFFサイズ  = dataSize + 36  [リトルエンディアン32bit整数]
 8 – 11     4B    フォーマット "WAVE"
12 – 15     4B    チャンクID  "fmt "
16 – 19     4B    fmtサイズ   = 16（非圧縮PCMの場合固定）
20 – 21     2B    音声形式    = 1（非圧縮リニアPCM）
22 – 23     2B    チャネル数  ch
24 – 27     4B    標本化周波数 fs  [Hz]
28 – 31     4B    データレート dr = fs × ch × (bit/8)  [B/s]
32 – 33     2B    ブロックサイズ bs = ch × (bit/8)  [B/標本]
34 – 35     2B    量子化深度  bit  [bit/ch]
36 – 39     4B    チャンクID  "data"
40 – 43     4B    dataサイズ  = len × ch × (bit/8)  [B]
```

### 8.4 hexdump 生データ（先頭48バイト）

```
=== 8bit, mono, fs=48000 ===
00000000  52 49 46 46 a4 bb 00 00  57 41 56 45 66 6d 74 20  |RIFF....WAVEfmt |
00000010  10 00 00 00 01 00 01 00  80 bb 00 00 80 bb 00 00  |................|
00000020  01 00 08 00 64 61 74 61  80 bb 00 00 ...          |....data........|

=== 16bit, mono, fs=48000 ===
00000000  52 49 46 46 24 77 01 00  57 41 56 45 66 6d 74 20  |RIFF$w..WAVEfmt |
00000010  10 00 00 00 01 00 01 00  80 bb 00 00 00 77 01 00  |.............w..|
00000020  02 00 10 00 64 61 74 61  00 77 01 00 ...          |....data.w......|

=== 24bit, mono, fs=48000 ===
00000000  52 49 46 46 a4 32 02 00  57 41 56 45 66 6d 74 20  |RIFF.2..WAVEfmt |
00000010  10 00 00 00 01 00 01 00  80 bb 00 00 80 32 02 00  |.............2..|
00000020  03 00 18 00 64 61 74 61  80 32 02 00 ...          |....data.2......|

=== 32bit, mono, fs=48000 ===
00000000  52 49 46 46 24 ee 02 00  57 41 56 45 66 6d 74 20  |RIFF$...WAVEfmt |
00000010  10 00 00 00 01 00 01 00  80 bb 00 00 00 ee 02 00  |................|
00000020  04 00 20 00 64 61 74 61  00 ee 02 00 ...          |.. .data........|

=== 16bit, stereo, fs=48000 ===
00000000  52 49 46 46 24 ee 02 00  57 41 56 45 66 6d 74 20  |RIFF$...WAVEfmt |
00000010  10 00 00 00 01 00 02 00  80 bb 00 00 00 ee 02 00  |................|
00000020  04 00 10 00 64 61 74 61  00 ee 02 00 ...          |....data........|

=== 16bit, mono, fs=8000 ===
00000000  52 49 46 46 a4 3e 00 00  57 41 56 45 66 6d 74 20  |RIFF.>..WAVEfmt |
00000010  10 00 00 00 01 00 01 00  40 1f 00 00 80 3e 00 00  |........@.......|
00000020  02 00 10 00 64 61 74 61  80 3e 00 00 ...          |....data.>......|

=== 16bit, mono, fs=44100 ===
00000000  52 49 46 46 ac 58 01 00  57 41 56 45 66 6d 74 20  |RIFF.X..WAVEfmt |
00000010  10 00 00 00 01 00 01 00  44 ac 00 00 88 58 01 00  |........D....X..|
00000020  02 00 10 00 64 61 74 61  88 58 01 00 ...          |....data.X......|
```

### 8.5 各フィールドの数値比較表

| ファイル | bytes 4–7 (RIFFサイズ) | bytes 22–23 (ch) | bytes 24–27 (fs) | bytes 28–31 (dr) | bytes 32–33 (bs) | bytes 34–35 (bit) | bytes 40–43 (dataサイズ) |
|---------|----------------------|-----------------|-----------------|-----------------|-----------------|-----------------|------------------------|
|  8bit, mono,   48kHz | 0x0000bba4 (48036) | 0x0001 (1) | 0x0000bb80 (48000) | 0x0000bb80 (48000) | 0x0001 (1) | 0x0008 (8)  | 0x0000bb80 (48000) |
| 16bit, mono,   48kHz | 0x00017724 (96036) | 0x0001 (1) | 0x0000bb80 (48000) | 0x00017700 (96000) | 0x0002 (2) | 0x0010 (16) | 0x00017700 (96000) |
| 24bit, mono,   48kHz | 0x000232a4 (144036)| 0x0001 (1) | 0x0000bb80 (48000) | 0x00023280 (144000)| 0x0003 (3) | 0x0018 (24) | 0x00023280 (144000)|
| 32bit, mono,   48kHz | 0x0002ee24 (192036)| 0x0001 (1) | 0x0000bb80 (48000) | 0x0002ee00 (192000)| 0x0004 (4) | 0x0020 (32) | 0x0002ee00 (192000)|
| 16bit, stereo, 48kHz | 0x0002ee24 (192036)| 0x0002 (2) | 0x0000bb80 (48000) | 0x0002ee00 (192000)| 0x0004 (4) | 0x0010 (16) | 0x0002ee00 (192000)|
| 16bit, mono,    8kHz | 0x00003ea4 (16036) | 0x0001 (1) | 0x00001f40 (8000)  | 0x00003e80 (16000) | 0x0002 (2) | 0x0010 (16) | 0x00003e80 (16000) |
| 16bit, mono,  44.1kHz| 0x000158ac (88236) | 0x0001 (1) | 0x0000ac44 (44100) | 0x00015888 (88200) | 0x0002 (2) | 0x0010 (16) | 0x00015888 (88200) |

### 8.6 考察：属性変化とヘッダの関係

#### (1) 量子化深度を変えたとき（8/16/24/32bit，モノラル，48kHz）

変化するフィールド：`dr`，`bs`，`bit`，`RIFFサイズ`，`dataサイズ`

```
bytes 34–35 (bit)：
  08bit → 0x08
  16bit → 0x10   ← 2倍
  24bit → 0x18   ← 3倍
  32bit → 0x20   ← 4倍

bytes 32–33 (bs = ch × bit/8)：
  08bit → 0x01 (1 B/標本)
  16bit → 0x02 (2 B/標本)
  24bit → 0x03 (3 B/標本)
  32bit → 0x04 (4 B/標本)

bytes 28–31 (dr = fs × bs)：
  08bit → 48000 B/s
  16bit → 96000 B/s  ← 2倍
  24bit → 144000 B/s ← 3倍
  32bit → 192000 B/s ← 4倍

bytes 40–43 (dataサイズ = len × bs)：
  08bit → 48000 B
  16bit → 96000 B  ← 2倍（ファイルサイズも2倍）
  24bit → 144000 B ← 3倍
  32bit → 192000 B ← 4倍

変化しないフィールド：ch (0x01)，fs (0x0000bb80)
```

**→ 量子化深度を上げるとデータ量はbit/8に比例して増える．`fs`は変化しない．**

#### (2) チャネル数を変えたとき（16bit，mono vs stereo，48kHz）

変化するフィールド：`ch`，`dr`，`bs`，`RIFFサイズ`，`dataサイズ`

```
bytes 22–23 (ch)：
  mono   → 0x0001
  stereo → 0x0002

bytes 32–33 (bs = ch × bit/8)：
  mono   → 0x0002 (2 B/標本)
  stereo → 0x0004 (4 B/標本)  ← 2倍（LとRを1標本にパッキング）

bytes 28–31 (dr = fs × bs)：
  mono   → 96000 B/s
  stereo → 192000 B/s  ← 2倍

変化しないフィールド：fs (0x0000bb80)，bit (0x0010)
```

**→ ステレオはモノラルの2倍のデータ量．標本は L・R 交互にパッキングされる（インターリーブ）．**

```
バイナリ配置：
  mono   : [L0][L1][L2][L3]...
  stereo : [L0 R0][L1 R1][L2 R2]...  ← bs=4B ごとにLR 1組
```

#### (3) 標本化周波数を変えたとき（16bit，mono，8000/44100/48000 Hz）

変化するフィールド：`fs`，`dr`，`RIFFサイズ`，`dataサイズ`

```
bytes 24–27 (fs)：
  8000  Hz → 0x00001f40
  44100 Hz → 0x0000ac44
  48000 Hz → 0x0000bb80

bytes 28–31 (dr = fs × bs)：
  8000  Hz → 16000 B/s
  44100 Hz → 88200 B/s
  48000 Hz → 96000 B/s

bytes 40–43 (dataサイズ = len × bs，len = fs × 1秒)：
  8000  Hz → 16000 B  （ファイルサイズ最小）
  44100 Hz → 88200 B
  48000 Hz → 96000 B

変化しないフィールド：ch (0x0001)，bs (0x0002)，bit (0x0010)
```

**→ `fs` が高いほど1秒あたりの標本数が多くなり，音質は上がるがファイルサイズも増える．**
**`bs`（ブロックサイズ）は ch と bit のみに依存するため fs には無関係．**

#### (4) 32bit-mono と 16bit-stereo の比較

```
bytes 4–7  (RIFFサイズ)：両方とも 192036 → 同じ
bytes 40–43 (dataサイズ)：両方とも 192000 → 同じ

ただし：
  32bit-mono  ：bs=4 (1ch × 4B/sample)，bit=32，ch=1
  16bit-stereo：bs=4 (2ch × 2B/sample)，bit=16，ch=2

→ ファイルサイズが等しくても，格納されている情報の意味が異なる．
   ヘッダの ch と bit を見ないと正しく解釈できない．
```

### 8.7 まとめ：ヘッダ変化の全体像

```
変更する属性    影響を受けるフィールド
─────────────────────────────────────────────
量子化深度 bit → bit(34-35), bs(32-33), dr(28-31), dataサイズ(40-43), RIFFサイズ(4-7)
チャネル数 ch  → ch(22-23),  bs(32-33), dr(28-31), dataサイズ(40-43), RIFFサイズ(4-7)
標本化周波数 fs → fs(24-27),            dr(28-31), dataサイズ(40-43), RIFFサイズ(4-7)

変更する属性    影響を受けないフィールド
─────────────────────────────────────────────
量子化深度 bit → ch, fs は不変
チャネル数 ch  → fs, bit は不変
標本化周波数 fs → ch, bs, bit は不変

type(20-21) と fmtSize(16-19) は常に固定（1 と 16）
```

---

## 9. rate による再生速度変更の実験

### 9.1 実験手順

元ファイル `out_16bit_mono_48k.wav`（440 Hz 正弦波，1秒，fs=48000）に対して，
`rate` アプリで倍率 0.25/0.5/1.0/2.0/4.0 を適用した5つのWAVファイルを生成した．

```bash
export LD_LIBRARY_PATH=./pcm/
./pcm/rate 0.25 < wav/out_16bit_mono_48k.wav > wav/rate_0.25x.wav
./pcm/rate 0.5  < wav/out_16bit_mono_48k.wav > wav/rate_0.5x.wav
./pcm/rate 1.0  < wav/out_16bit_mono_48k.wav > wav/rate_1.0x.wav
./pcm/rate 2.0  < wav/out_16bit_mono_48k.wav > wav/rate_2.0x.wav
./pcm/rate 4.0  < wav/out_16bit_mono_48k.wav > wav/rate_4.0x.wav
```

### 9.2 ヘッダの hexdump 比較（bytes 24–43）

`rate.c` が変更するのは `p->fmt.fs` の1フィールドのみ．bytes 24–27 のみ変化する．

```
倍率    bytes 24-27 (fs)  bytes 28-31 (dr)  bytes 32-33 (bs) bytes 34-35 (bit)
──────────────────────────────────────────────────────────────────────────────
0.25x   e0 2e 00 00       00 77 01 00        02 00             10 00
0.5x    c0 5d 00 00       00 77 01 00        02 00             10 00
1.0x    80 bb 00 00       00 77 01 00        02 00             10 00
2.0x    00 77 01 00       00 77 01 00        02 00             10 00
4.0x    00 ee 02 00       00 77 01 00        02 00             10 00
        ^^^^^^^^^^^^      ^^^^^^^^^^^^
        ここだけ変化        ここは不変（rate.c のバグ）
```

**注意：`dr`（データレート）は更新されていない**

`rate.c` は `p->fmt.fs` しか書き換えないため，`dr`（= fs × bs）はすべて元の値 96000 B/s のままとなっている．
仕様上は `dr = new_fs × bs` に更新すべきだが，多くのプレイヤーは `fs` と `data_size / (fs × bs)` から再生時間を計算するため，実用上は問題ない．

### 9.3 ffprobe による属性解析

```
ファイル        sample_rate   duration   認識されるピッチ
──────────────────────────────────────────────────────
rate_0.25x.wav   12000 Hz     4.000 s    110 Hz（440 × 0.25）
rate_0.5x.wav    24000 Hz     2.000 s    220 Hz（440 × 0.5）
rate_1.0x.wav    48000 Hz     1.000 s    440 Hz（基準）
rate_2.0x.wav    96000 Hz     0.500 s    880 Hz（440 × 2.0）
rate_4.0x.wav   192000 Hz     0.250 s   1760 Hz（440 × 4.0）
```

再生時間は `標本数 / fs = 48000 / new_fs` で計算される．

### 9.4 全倍率の定量比較

| 倍率 | fs [Hz] | 実際の再生時間 | 知覚されるピッチ | ピッチ変化 |
|------|---------|-------------|---------------|---------|
| 0.25x | 12000 | 4.000 s | 110 Hz | −24.0 半音（2オクターブ下） |
| 0.5x | 24000 | 2.000 s | 220 Hz | −12.0 半音（1オクターブ下） |
| 1.0x | 48000 | 1.000 s | 440 Hz | ±0 半音（原音） |
| 2.0x | 96000 | 0.500 s | 880 Hz | +12.0 半音（1オクターブ上） |
| 4.0x | 192000 | 0.250 s | 1760 Hz | +24.0 半音（2オクターブ上） |

ピッチ変化（半音）= 12 × log₂(rate)

### 9.5 スペクトログラムによる視覚的確認（ffmpeg showspectrumpic）

```bash
ffmpeg -i rate_1.0x.wav -lavfi "showspectrumpic=s=800x300:color=rainbow:scale=log:fscale=log" spectrum_1.0x.png
```

縦軸が周波数（対数スケール），横軸が時間．エネルギー集中帯（赤〜橙の帯）が知覚ピッチに対応する．

**0.25x（fs=12000 Hz）：4秒間，ピッチ 110 Hz**

```
時間軸：0〜4.0s（長い）
周波数帯：91〜196 Hz 付近（低い）← 110 Hz 付近に集中
```

**0.5x（fs=24000 Hz）：2秒間，ピッチ 220 Hz**

```
時間軸：0〜2.0s
周波数帯：110〜258 Hz 付近 ← 220 Hz 付近に集中
```

**1.0x（fs=48000 Hz）：1秒間，ピッチ 440 Hz（基準）**

```
時間軸：0〜1.0s（基準）
周波数帯：341〜877 Hz 付近 ← 440 Hz 付近に集中
```

**2.0x（fs=96000 Hz）：0.5秒間，ピッチ 880 Hz**

```
時間軸：0〜0.5s（半分）
周波数帯：450〜1270 Hz 付近 ← 880 Hz 付近に集中
```

**4.0x（fs=192000 Hz）：0.25秒間，ピッチ 1760 Hz**

```
時間軸：0〜0.25s（短い）
周波数帯：1838〜5691 Hz 付近 ← 1760 Hz 付近に集中
```

スペクトログラムの赤い帯が，倍率に比例して高周波方向へシフトしていることが確認できる．

### 9.6 考察まとめ

```
rate 処理の本質：
  WAVヘッダの fs を書き換える → プレイヤーの標本消費速度が変わる

  再生速度 ∝ new_fs / orig_fs = rate
  知覚ピッチ ∝ new_fs / orig_fs = rate
  再生時間 ∝ orig_fs / new_fs = 1/rate

  → 速度・ピッチ・時間長がすべて rate 倍で連動変化（テープ早送りと同原理）

注意点：
  rate.c は dr（データレート）を更新しない
  → ヘッダが厳密には不正確だが，多くのプレイヤーは fs ベースで解釈するため動作に支障なし
  → 完全に正しいヘッダにするには p->fmt.dr = p->fmt.fs * p->fmt.bs も更新が必要
```
